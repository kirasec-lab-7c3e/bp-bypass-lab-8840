queue_rules:
  - name: default
    queue_conditions:
      - "#approved-reviews-by>=1"
    merge_conditions:
      - check-success=slow-ci
pull_request_rules:
  - name: autoqueue-approved
    conditions:
      - base=release
      - "#approved-reviews-by>=1"
    actions:
      queue:
        name: default
