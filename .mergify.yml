pull_request_rules:
  - name: expand secret team into reviewers
    conditions:
      - base=main
      - -closed
    actions:
      request_reviews:
        users_from_teams:
          - "@kirasec-lab-7c3e/hidden-crew"
